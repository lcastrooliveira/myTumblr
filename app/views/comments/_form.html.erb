<%= form_for([@post, @post.comments.build]) do |f| %>
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %><br>
    </p>
    <p>
      <%= f.label :body %><br>
      <%= f.text_area :body %><br>
    </p>
    <br>
    <p>
      <%= f.submit %>
    </p>
<% end %>